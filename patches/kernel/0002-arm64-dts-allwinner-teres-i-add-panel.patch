From: User <user@example.com>
Subject: [PATCH] arm64: dts: allwinner: teres-i: add panel for ANX6345 eDP bridge

Add the innolux,n116bge panel node in chosen and connect it to anx6345.
Without a panel node, the DRM subsystem won't initialize the display
pipeline. Also add the port@1 endpoint on anx6345 to connect to the panel.

Signed-off-by: User <user@example.com>
---
 arch/arm64/boot/dts/allwinner/sun50i-a64-teres-i.dts | 23 +++++++++++++++++++++
 1 file changed, 23 insertions(+)

diff --git a/arch/arm64/boot/dts/allwinner/sun50i-a64-teres-i.dts b/arch/arm64/boot/dts/allwinner/sun50i-a64-teres-i.dts
index abcdef1..1234567 100644
--- a/arch/arm64/boot/dts/allwinner/sun50i-a64-teres-i.dts
+++ b/arch/arm64/boot/dts/allwinner/sun50i-a64-teres-i.dts
@@ -35,6 +35,16 @@
 			eDP12-supply = <&reg_dldo3>;
 		};
 
+		panel {
+			compatible = "innolux,n116bge", "simple-panel";
+			power-supply = <&reg_dldo2>;
+			enable-gpios = <&pio 3 23 GPIO_ACTIVE_HIGH>;
+
+			port {
+				panel_in: endpoint {
+					remote-endpoint = <&anx6345_out>;
+				};
+			};
+		};
 	};
 
 	gpio-keys {
@@ -145,6 +155,13 @@
 				anx6345_in: endpoint {
 					remote-endpoint = <&tcon0_out_anx6345>;
 				};
 			};
+
+			port@1 {
+				reg = <1>;
+
+				anx6345_out: endpoint {
+					remote-endpoint = <&panel_in>;
+				};
+			};
 		};
 	};
 };
